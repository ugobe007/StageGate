# ReadyForRobots OEM prospect sync (robot_companies → StageGate prospects)
RFR_API_BASE=https://ready-2-robot.fly.dev
RFR_SYNC_ENABLED=true

# Orbital AI Cloud — fleet monitor/control embedded at /admin/orbital.
# When unset, the Orbital page renders a "not connected" state (no errors).
# ORBITAL_API_URL=https://orbital.onstage.bot
# ORBITAL_API_KEY=
